/**
 * Copy this file to form-config.js and fill in your deployed Web App URL.
 * formSecret must match the FORM_SECRET script property in Apps Script (optional but recommended).
 */
window.SHEET_FORM_CONFIG = {
    webAppUrl: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',
    formSecret: 'paste-the-same-random-string-as-in-script-properties'
};
